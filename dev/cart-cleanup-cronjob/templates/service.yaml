apiVersion: v1
kind: Service
metadata:
  name: {{ include "cart-cleanup-cronjob.fullname" . }}
  labels:
    {{- include "cart-cleanup-cronjob.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cart-cleanup-cronjob.selectorLabels" . | nindent 4 }}
