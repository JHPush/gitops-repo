apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inkcloud-front.fullname" . }}-test-connection"
  labels:
    {{- include "inkcloud-front.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inkcloud-front.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
