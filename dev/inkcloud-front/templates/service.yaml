apiVersion: v1
kind: Service
metadata:
  name: {{ include "inkcloud-front.fullname" . }}
  labels:
    {{- include "inkcloud-front.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inkcloud-front.selectorLabels" . | nindent 4 }}
